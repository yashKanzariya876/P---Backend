# Backend Series

This is a Backend Learning.

Nodemon is use for server restart Means when file is saved then server is restart so we not need to start server manually.

We need to add dev dependency in scripts.